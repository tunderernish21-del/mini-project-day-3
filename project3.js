function setup() {
  createCanvas(1300,1300);
  background(0);
}

function draw() {
  noStroke(0);
  fill(245, 246, 247)
  ellipse(mouseX, mouseY, 25)
}

function mousePressed() {
  background(47, 189, 186);
}